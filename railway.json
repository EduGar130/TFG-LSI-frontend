{
  "build": {
    "command": "npm install && npm run build -- --configuration production",
    "output": "dist/inventario-app/browser"
  },
  "deploy": {
    "startCommand": "npx serve dist/inventario-app/browser"
  }
}
